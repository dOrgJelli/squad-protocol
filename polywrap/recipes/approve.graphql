mutation approve($address: String!, $spender: String!, $amount: BigInt!) {
  approve(
    address: $address
    spender: $spender
    amount: $amount
    connection: {
      networkNameOrChainId: $network
    }
  )
}
